import { TInlineStyles } from '@src/types/styles.type';

const spinnerSx: TInlineStyles = () => ({
  position: 'absolute',
  bottom: 0,
  left: 0,
  right: 0,
});

const PleaseWaitStyles = {
  spinnerSx,
};

export default PleaseWaitStyles;
